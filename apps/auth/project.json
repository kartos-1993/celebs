{
  "name": "auth",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/auth/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/auth",
        "main": "apps/auth/src/main.ts",
        "tsConfig": "apps/auth/tsconfig.app.json",
        "assets": []
      },
      "configurations": {
        "development": {
          "declaration": true,
          "declarationMap": true,
          "sourceMap": true
        },
        "production": {
          "declaration": true,
          "declarationMap": false,
          "sourceMap": false
        },
        "staging": {
          "declaration": true,
          "declarationMap": false,
          "sourceMap": false
        }
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "production",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "auth:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": {
          "buildTarget": "auth:build:development"
        },
        "production": {
          "buildTarget": "auth:build:production"
        },
        "staging": {
          "buildTarget": "auth:build:staging"
        }
      }
    },

    "serve:nodemon": {
      "executor": "nx:run-commands",
      "options": {
        "command": "dotenv -e apps/auth/.env.development -- nodemon --watch apps/auth/src --ext ts --exec node -r @swc-node/register apps/auth/src/main.ts"
      }
    },

    "migrate-dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "dotenv -e apps/auth/.env.development -- npx prisma migrate dev --schema=apps/auth/src/db/schema.prisma --name auto"
      }
    },
    "migrate-deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "dotenv -e apps/auth/.env.staging -- npx prisma migrate deploy --schema=apps/auth/src/db/schema.prisma"
      }
    },
    "studio": {
      "executor": "nx:run-commands",
      "options": {
        "command": "dotenv -e apps/auth/.env.development -- npx prisma studio --schema=apps/auth/src/db/schema.prisma"
      }
    },
    "prisma-generate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "dotenv -e apps/auth/.env.development -- npx prisma generate --schema=apps/auth/src/db/schema.prisma"
      }
    }
  },
  "tags": []
}
