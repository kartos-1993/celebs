/**
 * @deprecated Legacy monolithic Zustand cart store.
 * Cart server state is now managed via TanStack Query (src/features/cart/hooks/use-cart-queries.ts).
 * For client-side UI selection state, use `useCartUiStore`.
 */
export { useCartUiStore } from './use-cart-ui-store';
