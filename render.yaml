# Exported from Render on 2025-12-31T16:36:13Z
version: '1'
projects:
  - name: auth
    environments:
      - name: Production
        services:
          - type: web
            name: celebs
            runtime: docker
            repo: https://github.com/kartos-1993/celebs
            branch: staging-auth
            plan: free
            envVars:
              - key: APP_ORIGIN
                sync: false
              - key: SUPABASE_KEY
                sync: false
              - key: SUPABASE_URL
                sync: false
              - key: NODE_ENV
                sync: false
              - key: PORT
                sync: false
              - key: SMTP_FROM
                sync: false
              - key: SMTP_PASS
                sync: false
              - key: SMTP_USER
                sync: false
              - key: SMTP_PORT
                sync: false
              - key: SMTP_HOST
                sync: false
              - key: JWT_REFRESH_SECRET
                sync: false
              - key: JWT_REFRESH_EXPIRES_IN
                sync: false
              - key: JWT_EXPIRES_IN
                sync: false
              - key: JWT_SECRET
                sync: false
              - key: REDIS_PASSWORD
                sync: false
              - key: REDIS_PORT
                sync: false
              - key: REDIS_HOST
                sync: false
              - key: DATABASE_URL
                sync: false
            region: singapore
            dockerContext: .
            dockerfilePath: Dockerfile
            autoDeployTrigger: commit
            rootDir: .
